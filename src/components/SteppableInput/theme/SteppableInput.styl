.container {
	display: flex;
	align-items: center;
}

.input {
	flex: 1;
	background-color: transparent;
	border: none;
	padding: 0;
	font-family: inherit;
	font-size: inherit;
	line-height: inherit;
	width: 100%;
	display: block;
	outline: none;
}