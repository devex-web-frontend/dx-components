.container {
	position: relative;

	.input {
		position: relative;
		width: 100%;
	}
}

.anchor__caret {
	z-index: 2
}

.selectbox {
	position: absolute;
	top: 0;
	left: 0;
	width: 100%;
	height: 100%;
	overflow: hidden;
}

