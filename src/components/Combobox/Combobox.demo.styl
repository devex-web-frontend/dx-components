@require './Combobox.styl';

.container {
	width: 200px;
	height: 30px;

	.input {
		background: #eee;
		color: #000;
		box-shadow: none;
		padding-right: 25px;
		height: 100%;
	}

	&__anchor {
		padding-right: 0;

		&__wrapperCaret {
			width: 22px;
		}
	}
}
