@require './Combobox.styl';

.container {
	width: 200px;
	height: 30px;

	.input {
		background: #eee;
		color: #000;
		box-shadow: none;
		padding-right: 25px;
	}

	&__anchor {
		padding-right: 0;

		&__caret {
			width: 22px;
		}
	}
}
